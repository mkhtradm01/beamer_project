import 'package:flutter/material.dart';

class FeatureTwo extends StatelessWidget {
  const FeatureTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FeatureTwo'),
      ),
    );
  }
}
